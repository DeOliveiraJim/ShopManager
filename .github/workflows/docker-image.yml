name: Docker Image CI

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:

  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build -t jhimjhim/springbootmysql .
