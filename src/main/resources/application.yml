spring:
  jpa.hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost/shop_manager?createDatabaseIfNotExist=true&autoReconnect=true
    username: phpmyadmin
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql.init:
    platform: mysql
    mode: always
  jackson.deserialization.fail-on-unknown-properties: true
